spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/mytestdb?useUnicode=true&characterEncoding=UTF-8&useSSL=false
    driverClassName: com.mysql.jdbc.Driver
    username: root
    password: root
    type: com.alibaba.druid.pool.DruidDataSource

mybatis:
  config-locations: mybatis/mybatis-config.mapper
  mapper-locations: mybatis/mapper/*.mapper
  type-aliases-package: norestful